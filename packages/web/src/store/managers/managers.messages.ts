import { defineMessages } from 'react-intl';

export const managersMessages = defineMessages({
  failedToGetUsersList: {
    id: 'users.get-list-failed',
    defaultMessage: 'Failed to get managers list',
  },
});
