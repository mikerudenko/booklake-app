import { defineMessages } from 'react-intl';

export const appTableMessages = defineMessages({
  noData: {
    id: 'app-table.no-data',
    defaultMessage: 'No data',
  },
  noMatches: {
    id: 'app-table.no-matches',
    defaultMessage: 'No matches',
  },
});
