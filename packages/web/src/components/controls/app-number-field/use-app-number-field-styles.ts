import { makeStyles } from '@material-ui/styles';

export const useAppNumberInputStyles = makeStyles({
  formControl: {
    width: '100%',
    height: 80,
    marginBottom: 10,
  },
  input: {
    width: '100%',
  },
});
