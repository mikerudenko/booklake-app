import { makeStyles, Theme, createStyles } from '@material-ui/core/styles';

export const useLakesStyles = makeStyles((theme: Theme) =>
  createStyles({
    container: {
      paddingTop: 40,
    },
  }),
);
